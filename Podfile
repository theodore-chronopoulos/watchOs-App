# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!
def shared_pods
  pod 'Firebase/Auth'
  # pod 'Firebase/Firestore'
end

target 'Workout' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  platform :ios, '12.0'
  shared_pods
  # Pods for Workout

end

target 'Workout WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  platform :watchos, '8.0'
  shared_pods
  # Pods for Workout WatchKit App

end

target 'Workout WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  platform :watchos, '8.0'
  shared_pods

  # Pods for Workout WatchKit Extension

end
